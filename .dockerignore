# Build context for the dashboard image is the repo root (compose: context: ..).
# Keep host build output / deps out so the image builds clean and small.
**/node_modules
**/dist
**/dist-server
**/*.log
**/.env
**/.env.*
.git
.claude

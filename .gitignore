# Env / secrets
.env
.env.local
.env.*.local
*.pem
*.key
# PWA access whitelist (live allow-list; only the .example is tracked)
compose/oauth2-proxy-emails.txt

# Node / SvelteKit (Phase 2+)
node_modules/
.svelte-kit/
build/
.vercel/
dist/
*.log

# Python (migration scripts, hooks)
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Local backups / test dumps
*.rdb
*.sql.gz

# Claude Code worktrees (bg-job isolation; never commit)
.claude/worktrees/

# Claude Code task tracking (session-local; never commit)
.tasks/
